name: Python CI

on:
  push:
    branches:
      - main
      - develop
      - feature/*
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Notify Slack - Inicio del pipeline 🚀
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"🔄 Se inició un nuevo pipeline de CI para *${{ github.repository }}* en la rama *${{ github.ref_name }}*"}' \
          ${{ secrets.SLACK_WEBHOOK }}

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest

      - name: Lint with flake8
        run: |
          flake8 .

  notify-slack-success:
    needs: test
    if: success()
    runs-on: ubuntu-latest
    steps:
      - name: Notificar a Slack si fue exitosa la ejecución de la build 
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{
            "text":"*🎉 ¡Build completada exitosamente!* 🎉\n• Repositorio: `${{ github.repository }}`\n• Branch: `${{ github.ref_name }}`\n• Commit: `${{ github.sha }}`\n• Autor: `${{ github.actor }}`"
          }' \
          ${{ secrets.SLACK_WEBHOOK_URL }}

  notify-slack-on-failure:
    needs: test
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Notificar a Slack si falló la ejecución de la build
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{
            "text":"*❌ ¡La build ha fallado!* ⚠️\n• Repositorio: `${{ github.repository }}`\n• Branch: `${{ github.ref_name }}`\n• Commit: `${{ github.sha }}`\n• Autor: `${{ github.actor }}`"
          }' \
          ${{ secrets.SLACK_WEBHOOK_URL }}